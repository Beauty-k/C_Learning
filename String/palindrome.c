#include<stdio.h>
#include<string.h>
void Find_palindrome(char *str,int length){
    int i = 0;
    while(str[i] != '\0'){
        if(str[i] == str[length - i - 1]){
            
        }
    }
}
int main(){
    char str[6] = {"abccba"};
    Find_palindrome(str,6);
    
}